---
- hosts: localhost
  remote_user: root
  become: true
  roles:
    - ansible-roles-nodejs
  vars:
    nodejs_version: 10.x
    npm_packages:
      - http-server
      - typescript
      - webpack
      - less